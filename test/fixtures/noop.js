'use strict';

/**
* FUNCTION: noop( val )
*	A matrix function which does not change a matrix element.
*
* @param {Number} val - matrix value
* @returns {Number} matrix value
*/
function noop( val ) {
	return val;
}

// EXPORTS //

module.exports = noop;
